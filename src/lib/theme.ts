import { createTheme } from "@mui/material/styles";

// TODO: Add ligth & dark theme options
export const theme = createTheme({
  palette: {
    mode: "dark",
  },
  typography: {
    fontFamily: "Roboto, sans-serif",
  },
});
